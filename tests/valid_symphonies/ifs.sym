program ifs;
int a, b;
{
	a = 10;
	b = 20;

	if(a equals 10) {
	    // Executes
		print(1 + 1);
	};

	if(not (a equals 10)) {
	    // Doesn't execute
		print(1 + 1);
	};

	if(a equals 10) {
	    // Executes
		print(1 + 1);
	} elseif(not (a equals 10)) {
	    // Doesn't execute
	    print(1 + 1);
	};

	if(not (a equals 10)) {
	    // Executes
		print(1 + 1);
	} elseif(a equals 10) {
	    // Doesn't execute
	    print(1 + 1);
	};

	if(a equals 10) {
	    // Executes
		print(1 + 1);
	} elseif(not (a equals 10)) {
	    // Doesn't execute
	    print(1 + 1);
	} else {
	  	print(1 + 1);
	};

	if(not (a equals 10)) {
	    // Executes
		print(1 + 1);
	} elseif(not (a equals 10)) {
	    // Doesn't execute
	    print(1 + 1);
	} else {
	  	print(1 + 1);
	};
	
	/*
	if(not (a equals b)) {
		 print("pass");
	} elseif();
	elseif(false) {
		 ++a;
	};
	else {
		 --a;
	};
	*/
}