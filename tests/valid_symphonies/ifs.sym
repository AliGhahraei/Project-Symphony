program ifs;
int a, b;
{
	a = 10;
	b = 20;

	if(a equals 10) {
	    // Executes
		print("Hey");
	};

	if(not (a equals 10)) {
	    // Doesn't execute
		print("Hey");
	};

	if(a equals 10) {
	    // Executes
		print("Hey");
	} elseif(not (a equals 10)) {
	    // Doesn't execute
	    print("Hey");
	};

	if(not (a equals 10)) {
	    // Executes
		print("Hey");
	} elseif(a equals 10) {
	    // Doesn't execute
	    print("Hey");
	};

	if(a equals 10) {
	    // Executes
		print("Hey");
	} elseif(not (a equals 10)) {
	    // Doesn't execute
	    print("Hey");
	} else {
	  	print("Hey");
	};

	if(not (a equals 10)) {
	    // Executes
		print("Hey");
	} elseif(not (a equals 10)) {
	    // Doesn't execute
	    print("Hey");
	} else {
	  	print("Hey");
	};
	
	/*
	if(not (a equals b)) {
		 print("pass");
	} elseif();
	elseif(false) {
		 ++a;
	};
	else {
		 --a;
	};
	*/
}